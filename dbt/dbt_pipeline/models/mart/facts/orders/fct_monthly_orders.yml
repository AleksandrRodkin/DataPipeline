version: 2

models:
  - name: fct_monthly_orders
    description: "Number of completed orders in 30 days"

    columns:
      - name: order_date
        description: "The date on which the orders were created"
        data_tests:
          - unique
          - not_null
