version: 2

models:
  - name: fct_weekly_orders
    description: "Number of completed weekly orders"

    columns:
      - name: order_date
        description: "The date on which the orders were created"
        data_tests:
          - unique
          - not_null
